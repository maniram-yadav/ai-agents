{
    "mcpServers":{
        "mcp-project":{
            "command":"uv",
            "args":["run", "mcp-project"],
            "env":{
                 "PYTHONPATH": "${workspaceFolder}/src"
            }
        }
    }
}